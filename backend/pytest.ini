[pytest]
asyncio_mode = auto
markers =
    unit: Unit tests for isolated logic without external IO
    integration: Integration tests covering framework and IO collaboration
    application: Unit tests focused on application-layer coordination
    api: Tests targeting API-level contracts or DTOs
