# Server Configuration
HOST=0.0.0.0
PORT=8080
ENVIRONMENT=development

# Frontend origins allowed to call the API (comma separated)
CORS_ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:3001"]
# Allowed HTTP methods for CORS preflight (comma separated)
CORS_ALLOWED_METHODS=["GET","POST","PUT","PATCH","DELETE","OPTIONS"]
# Allowed headers forwarded from the browser (comma separated)
CORS_ALLOWED_HEADERS=["Authorization","Content-Type","Accept","Accept-Language","X-Requested-With"]

# Logging
LOG_LEVEL=INFO

# OpenAI API Key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY="your-openai-api-key-here"

# MCP (Model Context Protocol) Configuration
ENABLE_MCP_SYSTEM=true
MCP_NPX_COMMAND=npx
MCP_BASE_PATH=.
MCP_NODE_ENV=development
MCP_TIMEOUT_SECONDS=60

# Enabled MCP servers (comma separated list of server names)
MCP_ENABLED_SERVERS=
